import { HashLoader } from "react-spinners";
const Loader = () => {
  return (
    <div className="min-h-screen min-w-full flex justify-center items-center">
      <HashLoader color="#1084db" />
    </div>
  );
};

export default Loader;
